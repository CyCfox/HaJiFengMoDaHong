# 《哈基蜂摸大红》修订版完整实现计划

## 1. 总结与已确认决策

- 网页端单机肉鸽射击游戏：WASD 移动、武器自动索敌但子弹不追踪、敌人特色攻击、掉落/开箱/背包/手动撤离/特勤处/收藏室完整闭环。
- 技术栈：Phaser 3 + TypeScript + Vite；后端 Express + Node 内置 `node:sqlite`；仅持久化收藏室点亮藏品。
- 已锁定：无尽关卡、镜头跟随、四把核心武器、15 张正品藏品、17 个 BUFF、程序合成音效、单机单档案、初始背包 20 格、仓库无限格。
- 武器数量和种类没有上限：只要所有已装备武器总重量不超过负重即可继续装备；同种武器可重复购买、重复装备、分别升级和出售。
- G18 可以出售和卸下，但始终必须保留至少一把已装备武器；若尝试卸下或出售最后一把武器，操作被禁用并提示。
- 每关清完敌人后才出现撤离点，玩家可继续拾取和开箱，必须按 `F` 手动撤离；每 5 关出现赛伊德首领。
- 每次通关撤离并再次进入特勤处时，抽卡次数 `N` 重置为 0；在当前这次特勤处会话内切页、返回主菜单再进入都不重置，避免反复重进刷低价。
- 所有数值、配方、掉落权重和成长曲线集中在 `shared/balance` 配置中；业务层不写死数值。

## 2. 架构与持久化

- 工程结构：`src/` 为前端游戏与 DOM 界面、`server/` 为 Express API、`shared/` 为类型与配置、`assets/` 为已有素材、`tests/` 为自动化测试。
- Phaser 只负责战斗场景；主菜单、特勤处、设置、背包、抽卡、结算使用 HTML/CSS DOM 叠层；`GameBus` 解耦 Phaser 与 DOM，`RunStore` 保存当前局状态，`SaveGateway` 封装后端。
- 核心模式：`StateMachine`、`Strategy`、`Factory`、`ObjectPool`、`Repository`、`EventBus`；敌人行为、武器策略、BUFF 效果均通过策略注册表实现。
- 后端 API：
  - `GET /api/collections` → `{ "lit": ["maiden_pendant", ...] }`
  - `POST /api/collections/:id/light` → `{ "ok": true, "lit": [...] }`
  - 重复点亮返回成功且不重复写入；非法 ID 返回 400。
- SQLite 表：`lit_collections(id TEXT PRIMARY KEY, lit_at INTEGER NOT NULL)`；数据库默认路径 `server/data/hajifeng.sqlite`，可用 `HAJIFENG_DB_PATH` 覆盖。
- 点亮藏品时先成功调用后端，再更新前端收藏室；失败则保留仓库藏品并显示重试。
- 开发端口：Vite 5173、API 3001；生产环境 Express 同时托管 `dist` 和 `/api`。

## 3. 战斗系统

### 武器行为

- 每把武器是一个独立武器实例，拥有唯一实例 ID；购买同种武器会生成新实例，不自动合并。
- 武器围绕角色等分圆周固定：1 把在上方；2 把在上、下；3 把在上、左下、右下；更多武器继续按 `360° / 已装备数` 等分，装备数量不设上限。
- 武器自身位置为角色中心加固定轨道半径；武器索敌距离以该武器自身位置为圆心，不是以角色为圆心。
- 每把武器独立选择自身射程内最近的敌人，按 12 rad/s 平滑旋转到目标方向；射程内没有敌人时枪口保持默认方向且不开火。
- 开火瞬间锁定当前武器朝向，子弹从该朝向直线运动；子弹只保存固定 `direction`，不具备追踪、转向、锁定或修正功能。
- 多发子弹以开火时的敌人方向为对称轴扇形发射；总扩散角约 `弹数 × 8°`，不再二次修正方向。
- 同一武器可重复装备；相同武器实例各自独立冷却、独立索敌、独立射击。
- 重量限制只决定是否可装备，不限制可拥有或已购买数量；若总重量超过负重，购买后的武器保留在已购列表但无法装备。
- 至少一把武器保持装备；G18 可出售和卸下，只要出售/卸下后还有其他武器可装备。

### 武器配置

| 武器 | 购买价 | 射程 | 射速/秒 | 伤害 | 弹数 | 贯穿 | 重量 |
|---|---:|---:|---:|---:|---:|---:|---:|
| G18 手枪 | 120,000 | 420 | 2.0 | 12 | 1 | 0 | 3 |
| UZI 冲锋枪 | 350,000 | 520 | 7.5 | 6 | 1 | 0 | 5 |
| AKM 突击步枪 | 810,000 | 680 | 4.5 | 16 | 1 | 0 | 8 |
| AWM 狙击步枪 | 2,300,000 | 920 | 0.5 | 150 | 1 | 4 | 15 |

- 射程、射速、伤害三项可在武器强化中升级，属性各自独立等级、无固定上限；弹数不再通过武器强化提升，只能通过“额外弹巢”BUFF 增加。
- 每次升级只选择一个属性，该属性等级从 `Lv N` 提升到 `Lv N+1`；下一次继续升级该属性时按新等级计费。
- 单次升级费用：`武器基础购买价 × 0.1 × 该属性当前等级`，取整数。例如 G18 射程从 Lv1 升到 Lv2 消耗 12,000；Lv5 升 Lv6 消耗 60,000。
- 属性成长：射程每级 +5% 基础值，射速每级 +5% 基础值，伤害每级 +10% 基础值。
- 售出价格只按该武器实例的购买价 50% 计算，升级投入不计入售价。
- 金色/红色武器升级图标、属性等级、当前状态、重复实例编号均在交易行清晰显示；武器列表超出视口时使用滚轮滚动。

### 玩家、敌人与关卡

- 玩家：HP 100、护甲 60、移速 220、负重 18、背包 20 格，拾取半径 34；每关开始回满 HP/护甲。
- 第 N 关敌人数量严格按下表生成：
  - 小兵：`6 + 2N`
  - 盾兵：第 2 关起 `floor(N/2)`
  - 火箭兵：第 6 关起 `floor(N/2)`
  - 机枪兵：第 11 关起 `floor(N/2)`
  - 喷火兵：第 21 关起 `floor(N/2)`
  - 赛伊德：第 5、10、15……关额外增加 1 个，不作为其他敌人的替代。
- 所有敌人从地图各点位分批刷新；总数量固定，当前地图内敌人分批出现，保证不会一次性堆满。
- 敌人生命成长采用 `1.02^(N-1)`；伤害 `1 + 0.03×(N-1)`；射速 `1 + 0.01×(N-1)`，上限 3 倍；移速 `1 + 0.01×(N-1)`，上限 2 倍；所有倍率按 0.01 向下取整。
- 敌人基础数值与第一关配置：

| 敌人 | HP | 移速 | 射程 | 射速 | 伤害 | 特色 |
|---|---:|---:|---:|---:|---:|---|
| 阿萨拉小兵 | 50 | 105 | 380 | 0.55/s | 7 | 三连黄色枪弹 |
| 阿萨拉盾兵 | 180 | 70 | 300 | 0.8/s | 14 | 全身减伤 65%，盾击震荡弹 |
| 阿萨拉火箭兵 | 110 | 85 | 540 | 0.35/s | 50 | 军用炮弹，命中玩家或到达目标位置后爆炸 |
| 阿萨拉机枪兵 | 140 | 75 | 540 | 2.4/s | 10 | 连续蓝色弹流与火花 |
| 阿萨拉喷火兵 | 130 | 88 | 190 | 持续喷火 | 最大生命4%/0.2s | 火焰自动朝向玩家，持续命中伤害 |
| 赛伊德 | 3,000 | 65 | 650 | 随机技能 | 22 / 最大生命4% | 火焰箭扇、十向弹幕、15秒召唤五名随机小兵 |

- 盾兵全身受到玩家子弹伤害降低 65%；火箭兵发射军用炮弹，命中玩家或到达目标位置后播放 0.5 秒爆炸动画；喷火兵持续喷射朝向玩家的火焰，在范围内每 0.2 秒造成玩家最大生命 4% 伤害。首领无阶段切换，每次攻击在五枚火焰箭扇形与十向环形弹幕中随机释放，并每 15 秒随机召唤五名小兵。火焰箭到达目标后使用燃烧特效生成火焰区域，判定范围与火焰视觉大小一致。
- 所有非首领敌人使用 100%→112%→100% 的呼吸缩放动画；角色始终循环播放 11 帧 idle 动画，每 0.5 秒循环。
- 角色和敌人均使用圆形物理碰撞体，实体间持续推挤；地图四周为不可越界边界。
- 当前地图素材不含障碍碰撞数据，因此本期只实现地图边界和实体碰撞，不制作墙体/地形阻挡；后续配置化接入碰撞层。

### 藏品、背包与撤离

- 藏品仍使用 15 张 `carton` 正品，18 张 `collections/origin` 仅作设计备份，不进入游戏。

| 藏品 | 品质 | 参考价 | 占格 |
|---|---:|---:|---:|
| 女郎吊坠 | 蓝 | 12,000 | 1 |
| 牛角 | 紫 | 18,000 | 2 |
| 阿萨拉酒杯 | 金 | 63,296 | 1 |
| 赛伊德怀表 | 红 | 216,831 | 2 |
| 万足金条 | 红 | 333,903 | 4 |
| 黄金瞪羚 | 红 | 455,560 | 4 |
| 滑膛枪展品 | 红 | 669,130 | 4 |
| 留声机 | 红 | 1,282,036 | 4 |
| 半身像 | 红 | 1,326,548 | 3 |
| 印象派名画 | 红 | 2,105,569 | 9 |
| 主战坦克模型 | 红 | 2,142,119 | 9 |
| 万金泪冠 | 红 | 3,184,882 | 9 |
| 炫彩哈基蜂 | 红 | 5,888,888 | 2 |
| 非洲之心 | 红 | 13,141,314 | 1 |
| 海洋之泪 | 红 | 28,834,903 | 1 |

- 敌人掉落品质概率：蓝 60%、紫 25%、金 12%、红 3%；小保险箱 45/30/20/5%；大保险箱 20/30/35/15%。
- 红色内部权重仍从低到高安排：赛伊德怀表、万足金条、黄金瞪羚、滑膛枪展品、留声机、半身像、印象派名画、主战坦克模型、万金泪冠、炫彩哈基蜂、非洲之心、海洋之泪。
- 保险箱高价值红品的加权提升保留：小箱 ×3、大箱 ×5。
- 普通小兵掉落率 30%、精英 55%、首领 100%；掉落物留在地面，玩家进入拾取半径后自动拾取。
- 每关 3 个小保险箱；偶数关额外 1 个大保险箱；小箱 1.6 秒、大箱 3 秒开启，离开范围或受击退则进度暂停。
- 背包初始 20 格；仓库无限格。拾取时按藏品格数占用，空间不足时物品保留在地面并提示；`TAB` 打开背包，可选择藏品主动丢弃。
- 全图敌人清空后出现撤离点和方向指引；玩家可继续拾取、开箱；按 `F` 后进入 1.5 秒撤离读条，离开撤离点则取消，撤离成功才进入特勤处。
- 玩家在清场前或撤离前死亡，本局清空背包、仓库、武器、哈哈币和 BUFF，关卡重置回第 1 关，只保留已经提交并点亮过的收藏室。

## 4. BUFF 与抽卡

- 每次抽取从 17 个 BUFF 池中选 3 张不同卡牌；三张卡在一个样本内不重复，但跨抽卡允许重复出现并叠加。
- 抽卡采用玩家三选一：确认后只获得选中的 1 张；选中卡片边框变黄；不确认不扣币。
- 当前关卡撤离并进入特勤处时，`N=0`；本次特勤处会话内首次抽取费用 `50,000 × 2^0 = 50,000`，第 2 次 `100,000`，第三次 `200,000`；当前会话内持续递增。
- `N` 只在通关撤离后的下一次特勤处会话开始时重置，不在退出/进入特勤处页面时重置，也不在切导航时重置。
- 属性类 BUFF 可重复抽取和叠加：
  - +20 HP
  - +25 护甲
  - +10% 移速
  - +10 背包容量
  - +5 背包容量
  - +6 负重
  - +15% 武器伤害
  - +8% 武器射速
- 功能类 BUFF：拾取磁吸为唯一功能性 BUFF，获得后不再进入后续抽取池；其余功能类均可叠加：
  - 所有武器 +1 弹数
  - 所有子弹 +1 贯穿
  - 容器额外掉落 1 次
  - 红色藏品概率 +1%（在对应掉落来源的基础红率上直接加 1 个百分点，其余品质按原比例分摊剩余概率）
- 状态类 BUFF 均可在命中时叠加，且对每名敌人独立记录“效果结束后的 5 秒免疫间隔”：
  - 燃烧：每 0.5 秒造成 1% 敌人最大生命值，持续 2 秒；每次堆叠使每跳伤害 +1% 最大生命值；燃烧中敌人显示红色。燃烧与冰冻同时存在时按最新一次状态决定红/蓝颜色。
  - 冰冻：每堆叠加 20% 减速，持续 5 秒；堆叠为加法叠加，不设额外上限；冰冻中敌人显示蓝色。
  - 吸血：每堆使造成的伤害转化 2% 为生命回复，可多次触发。
  - 眩晕：每堆提供 5% 命中触发概率；成功触发后眩晕 1.5 秒，不叠加持续时间，只叠加触发概率。
- BUFF 面板显示名称、类型、当前层数、具体效果、敌人免疫倒计时；BUFF 全部在死亡后清空。

## 5. 界面、画面比例、音效与验收

- 主界面：游戏标题、开始游戏、特勤处、设置；开始游戏直接按初始 G18、0 币、第 1 关进入。
- 对局 HUD：HP/护甲、哈哈币、关卡、击杀、负重、背包、武器、BUFF、撤离方向；伴随浮动伤害、震屏、命中火花、枪口闪光。
- 特勤处导航：开始游戏、仓库、交易行、幸运鸟窝、收藏室；所有区域页面滚动，鼠标滚轮、自定义滚动条、选中态和悬停动效齐全。
- 交易行必须支持大量重复武器：按武器实例卡片展示，可购买、升级三项属性（射程/射速/伤害）、出售、装备、卸下；金额不足时按钮禁用并提示。
- 幸运鸟窝：抽取按钮、三张卡的选中黄框、确认、当前费用、BUFF 叠层显示。
- 收藏室：15 张卡片，点亮/未点亮、提交、副本不足、放大详情；提交成功后才移除仓库藏品并持久化。
- 对局内元素比例：地图世界坐标固定为 1024×1024；玩家显示高度约 36 世界像素，普通敌人约 32、精英约 38、首领约 72、武器约 30、藏品约 24、小箱约 42、大箱约 50；物理碰撞半径显示尺寸的 60% 左右，保证视觉与判定协调。
- 加载图片时按透明像素包围盒自动裁剪并缩放到目标尺寸，不直接使用 720×960、2048×2048 等原始尺寸；这样动画帧不会因留白导致抖动或比例失衡。
- 摄像机使用 `宽度/1024` 与 `高度/1024` 的较大值作为缩放，地图铺满浏览器可视区域，并平滑跟随玩家；Canvas 使用 `RESIZE + AutoCenter`，页面本身禁止滚动。
- 所有音效继续采用 Web Audio 程序合成，覆盖按钮、悬停、开火、命中、爆炸、拾取、开箱、出售、抽卡、提交、撤离、死亡、首领登场；设置中提供总音量、SFX、粒子质量和画面缩放。
- 开发过程中使用 Playwright 自动截图主界面、战斗、特勤处各页、抽卡、收藏室、首领战等场景，并生成像素/布局检查报告。

### 测试计划

- 单元测试：武器实例重复购买、重量装备判定、G18 出售/卸下、最后一把武器限制、等级费用、四项属性成长、出售价只含购买价一半、无追踪子弹轨迹。
- 敌人测试：第 1/2/6/11/21/N 关的数量公式、倍率向下取整、射速与移速上限、首领每 5 关触发、各敌人特效。
- 抽卡测试：三张不重复、跨次重复、属性/功能/状态叠加、5 秒敌人免疫、红色概率递增、当前特勤处会话计费与下一关重置。
- UI/E2E：主菜单→战斗→击杀→开箱→拾取→清场→`F` 撤离→仓库转移→出售→购买/升级/装备→抽 BUFF→提交收藏→下一关；以及死亡清空且收藏室仍保留。
- 视觉验收：1280×720、1600×900、1920×1080 下检查角色、地图、敌人、子弹、容器比例，确认无越界、无遮挡、无资源缺失、无控制台错误；60 FPS 性能目标。

### 假设与默认值

- 武器属性升级不设上限，靠指数费用自然限制；射程/射速/伤害各自从 Lv1 开始，弹数只能由 BUFF 增加。
- 出售武器只返还购买价的 50%，升级投入不返还。
- “至少装备一把武器”是软性校验，不是武器数量上限。
- BUFF 跨抽卡可重复获得并叠加，唯一例外是拾取磁吸。
- 状态叠加不设额外上限，眩晕持续时间固定 1.5 秒。
- 数据库和源码不包含任何官方音频、官方 UI 素材或侵权资源；仅使用项目现有图片与程序合成音频。
